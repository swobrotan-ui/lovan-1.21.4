import a.Loader;
import com.google.gson.reflect.TypeToken;
import java.util.List;

class fxro extends TypeToken<List<so>> {
   fxro(k k) {
   }

   static {
      Loader.init(fxro.class);
   }

   public static native void guard();
}
